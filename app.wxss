/* 微信小程序样式不支持通配符选择器 */
page,
view,
text,
swiper,
swiper-item,
image,
navigator {
    padding: 0;
    margin: 0;
    box-sizing: border-box;
}

/* 变量实现主题颜色 */
page {
    --themeColor: #e76a5d;
    font-size: 28rpx;
}
